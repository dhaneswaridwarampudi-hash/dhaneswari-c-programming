#include<stdio.h>
int main()
{
	int a,b,c,max;
	printf("enter the values of a,b,c");
	scanf("%d%d%d",&a,&b,&c);
	if (a<b&&b>c)
	{
		printf("b is max %d",b);
	}else if (b<a&&a>c){
	printf("a is max %d",a);
	} else {
		printf("c is max %d",c);
	}
}
